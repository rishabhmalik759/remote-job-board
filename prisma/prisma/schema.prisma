datasource db {
  provider = "postgres"
  url      = "postgresql://postgres:toor@localhost:5432/job-board"
}

generator client {
  provider = "prisma-client-js"
}
model Company{
  id        String   @id @default(uuid())
  companyName String @unique
}